{{#each products}}
    <div>
        <h2>{{this.title}}</h2>
        <p>{{this.description}}</p>
        <p>{{this.price}}</p>
        <a href="/products/{{this._id}}">Details</a>
        <hr>
    </div>
{{/each}}